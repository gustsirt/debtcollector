import { Router } from "express";

const router = Router();

export default router
  .get('/', () => {})
  .get('/:eid', () => {})
  .post('/', () => {})
  .put('/:eid', () => {})
  .delete('/:eid', () => {})